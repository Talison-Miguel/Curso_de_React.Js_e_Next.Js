import { H1 } from '../H1';
import { P } from '../P';

// eslint-disable-next-line react/prop-types
export const Div = ({ children }) => {
    return (
        <>
            <H1 />
            <P />
        </>
    );
};
